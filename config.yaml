name: Necrologi Zona TV
slug: necrologi_zona_tv
version: "0.1.0"
description: Raccoglie necrologi per Orsago, Cordignano, Godega, San Fior e li espone via API.
url: https://github.com/example/necrologi-addon
startup: services
boot: auto
init: false
arch:
  - aarch64
  - amd64
  - armv7
map:
  - share:rw
ports:
  8099/tcp: 8099
ports_description:
  8099/tcp: API HTTP necrologi
options:
  scan_interval_minutes: 60
  max_items_per_source: 80
  save_images: true
  towns:
    - Orsago
    - Cordignano
    - Godega
    - San Fior
  log_level: info
schema:
  scan_interval_minutes: int(10,1440)
  max_items_per_source: int(10,300)
  save_images: bool
  towns:
    - str
  log_level: list(trace|debug|info|warn|error)
